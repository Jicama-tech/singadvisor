# Deployment

Reserved for the combined Frontend + Backend deployment runbook and
auto-deploy tooling — Phase 5 of the *SingAdvisor Platform Modernization
Proposal* (client deliverable, kept outside this repo), mirroring
`eventsh-v1/deployment/` (`autodeploy.sh`, `SERVER_SETUP.md`,
`webhook-server.js`).

**Not built yet.** Until Phase 5 ships, the live production deploy is
documented in [`Frontend/DEPLOY.md`](../Frontend/DEPLOY.md) — it covers the
current single-service (Next.js only) deploy to `jicama.tech/singadvisor` and
predates this Frontend/Backend split.
