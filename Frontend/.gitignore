# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
next-env.d.ts

# database
*.db
*.db-journal
prisma/dev.db*

# uploads (candidate resumes) - personal data, stored outside public/
var/uploads/
var/

# env
.env
.env.local
.env*.local

# event-ops migration dump (one-off, regenerable via prisma/export-events.ts)
events-export.json

# misc
.DS_Store
*.pem
.vercel
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# legacy vite app (kept for reference, not built)
legacy/node_modules/
legacy/dist/
legacy/.vite/
legacy/.env
tsconfig.tsbuildinfo
